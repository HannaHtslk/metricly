// TODO: implement top navbar with theme toggle and user avatar/logout
interface NavbarProps {
  onToggleTheme: () => void;
}

const Navbar = (_props: NavbarProps) => {
  return <nav>Navbar</nav>;
};

export default Navbar;
